import {ChevronRight,Inbox} from 'lucide-react';import type {Contact} from '../types';
const color:Record<string,string>={'Institutional professional email':'bg-emerald-50 text-emerald-700','Generic program contact':'bg-sky-50 text-sky-700','Free-email address requiring review':'bg-amber-50 text-amber-800','Do Not Contact':'bg-red-50 text-red-700','Invalid email':'bg-red-50 text-red-700','Duplicate':'bg-slate-100 text-slate-700'};
export function ResultsTable({records,onSelect}:{records:Contact[];onSelect:(r:Contact)=>void}){return <section className="card overflow-hidden"><header className="border-b border-slate-200 p-5"><h2 className="font-bold">Contact records</h2><p className="text-sm text-slate-500">Review and approve each contact before export.</p></header>{records.length===0?<div className="flex flex-col items-center px-6 py-16 text-center"><span className="rounded-full bg-slate-100 p-4 text-slate-400"><Inbox size={28}/></span><h3 className="mt-4 font-semibold">No matching contacts</h3><p className="mt-1 max-w-sm text-sm text-slate-500">Add an authorized source or adjust your filters. Extracted records stay on this device.</p></div>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Name / role','Program','Institution','Professional email','Classification','Status',''].map(h=><th key={h} className="whitespace-nowrap px-5 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{records.map(r=><tr key={r.id} className="hover:bg-slate-50"><td className="px-5 py-4"><p className="font-medium">{r.full_name||'Program contact'}</p><p className="text-xs text-slate-500">{r.role||'—'}</p></td><td className="px-5 py-4">{r.training_program||'—'}</td><td className="px-5 py-4">{r.institution||'—'}</td><td className="px-5 py-4 font-medium text-navy-700">{r.professional_email}</td><td className="px-5 py-4"><span className={`badge whitespace-nowrap ${color[r.email_classification]}`}>{r.email_classification}</span></td><td className="px-5 py-4"><span className={`badge ${r.verification_status==='Approved'?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{r.verification_status}</span></td><td className="px-5 py-4"><button onClick={()=>onSelect(r)} aria-label={`View ${r.professional_email}`} className="rounded p-2 hover:bg-slate-100"><ChevronRight size={17}/></button></td></tr>)}</tbody></table></div>}</section>}
