import {describe,expect,it} from 'vitest';import {classifyEmail,deduplicateContacts,extractEmails,normalizeEmail} from './email';import type {Contact} from '../types';
describe('email utilities',()=>{it('normalizes case, whitespace, and mailto query',()=>expect(normalizeEmail(' Mailto:Person@Example.EDU?subject=Hi ')).toBe('person@example.edu'));it('extracts visible and mailto emails but excludes hidden regions',()=>{const html='<main><p>Coordinator: program@example.edu</p><a href="MAILTO:resident@example.edu">Contact</a><span hidden>hidden@example.edu</span></main><footer>noise@example.com</footer><script>script@example.com</script>';expect(extractEmails(html).map(x=>x.email).sort()).toEqual(['program@example.edu','resident@example.edu'])});it('classifies domains and generic mailboxes',()=>{expect(classifyEmail('resident@medical-example.edu')).toBe('Institutional professional email');expect(classifyEmail('program@medical-example.org')).toBe('Generic program contact');expect(classifyEmail('person@gmail.com')).toBe('Free-email address requiring review');expect(classifyEmail('bad')).toBe('Invalid email')});it('deduplicates and preserves sources',()=>{const base={id:'1',full_name:'',role:'',specialty:'',training_program:'',institution:'',country:'',professional_email:'Test@Example.edu',email_classification:'Institutional professional email',verification_status:'Unverified',source_url:'a',source_urls:['a'],collected_at:'',notes:'',evidence:'',extraction_method:''} as Contact;const result=deduplicateContacts([base,{...base,id:'2',source_url:'b',source_urls:['b']}]);expect(result).toHaveLength(1);expect(result[0].source_urls).toEqual(['a','b']);expect(result[0].email_classification).toBe('Duplicate')})});
