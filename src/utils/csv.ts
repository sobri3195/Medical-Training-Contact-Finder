import Papa from 'papaparse';
export const CSV_COLUMNS=['full_name','role','specialty','training_program','institution','country','professional_email','profile_url','source_url','notes'] as const;
export type CsvRow=Record<(typeof CSV_COLUMNS)[number],string>;
export function parseCsv(text:string):CsvRow[]{const parsed=Papa.parse<Record<string,string>>(text.replace(/^\uFEFF/,''),{header:true,skipEmptyLines:'greedy',transformHeader:h=>h.trim().toLowerCase()});if(!parsed.meta.fields?.includes('professional_email'))throw new Error('CSV must include a professional_email column.');const error=parsed.errors.find(item=>item.code!=='UndetectableDelimiter');if(error)throw new Error(error.message);return parsed.data.map(row=>Object.fromEntries(CSV_COLUMNS.map(k=>[k,(row[k]||'').trim()])) as CsvRow).filter(row=>Object.values(row).some(Boolean))}
export const toCsv=(rows:Record<string,string>[])=>Papa.unparse(rows,{columns:['full_name','role','specialty','training_program','institution','country','professional_email','profile_url','email_classification','verification_status','source_url','collected_at','notes'],escapeFormulae:true});
